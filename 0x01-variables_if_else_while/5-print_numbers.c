#include <stdio.h>

/**
* main - main function that prints numbers 0 through 9
*
* Return: 0
*/

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)

	printf("%d", num);
	putchar ('\n');
	return (0);
}
