#include "holberton.h"
/**
 * mul - a function that multiplies two integers
 * @a: number being multoplied
 * @b: number being multiplied
 * Return: a times b
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
